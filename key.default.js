// Créer un fichier key.js, et y coller le contenu
// de ce fichier en complétant la variable vide
// par une clée SSH à générer (par exemple sur https://8gwifi.org/sshfunctions.jsp)
const key = '';

module.exports = key;
